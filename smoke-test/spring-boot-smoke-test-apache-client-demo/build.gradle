plugins {
    id "java"
}

description = "Spring Boot Apache HttpClient demo smoke test"

dependencies {
    implementation(project(":starter:spring-boot-starter-web"))
    implementation(project(":module:spring-boot-http-client"))
    
    testImplementation(project(":starter:spring-boot-starter-test"))
}
